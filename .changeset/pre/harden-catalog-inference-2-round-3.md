---
"hejbro": patch
---

`import` and `pull` print their `Not inferred: no table or enum to declare in schema "X".` lines sorted by code units, each schema once, whatever the `--schema` order — the same order every other list in the loss report already uses — and the `*-nothing-to-infer` / `*-nothing-declarable` refusals list schemas the same way.
