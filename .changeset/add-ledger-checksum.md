---
"hejbro": minor
---

The ledger records a body checksum, migrate refuses an applied migration whose body changed, status reports it, and a ledger whose rows are filtered by row-level security is refused before it is read.
