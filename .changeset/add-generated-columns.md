---
"@hejbro/core": minor
---

Generated-family columns: declare stored computed columns with `.generatedAlwaysAs(...)` and identity columns with `.generatedAlwaysAsIdentity(options?)` / `.generatedByDefaultAsIdentity(options?)` (sequence options included) — emitted with the full Postgres grammar, diffed with precise identity alters, snapshot format version 6, and insert/update input types that exclude the columns Postgres itself refuses to write.
