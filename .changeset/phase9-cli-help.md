---
"@hejbro/core": patch
"hejbro": patch
"@hejbro/supabase": patch
---

`hejbro restore --help` documents the `<n>` positional; `hejbro --help` keeps each command on one line; `restore`'s undo hint notes that restored files are staged.
