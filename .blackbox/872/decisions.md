# Decisions — quickstart-now/hejbro#872

Every decision on this work item, appended as it is made: owner decisions (`D#`, English rewrites of the owner's words) and AI rulings (`R#`, with kind, basis and ratification). Managed by `blackbox add`; append-only.

<a id="r1"></a>
## R1 — #872 #873 #874 fold into harden-catalog-inference-2: dropped PK name announced, FK at an omitted column omitted and named, one code-point comparator

_lead · extension · basis 412/D24, D25; the cv reviewer's three constructor findings; the requirement's own omission rule for tables and schemas; check's settled code-point rule · 2026-09-05T06:15Z · ratified: pending_

Same files, same requirement, one piece: a PK whose name is not the derived one is announced with both ways out; a column omission takes its FKs in both directions and names them so the starter always loads; the loss report sorts by code points through the inventory's own comparator. Tasks 1.3/1.4 added; witness and docs renumbered. Ratification: owner on return.

