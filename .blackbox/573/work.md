# Work — quickstart-now/hejbro#573

What was built, measured and reversed under the decisions, one entry per PR or group (`W#`). Managed by `blackbox add work`; append-only.

<a id="w1"></a>
## W1 — refuse-nile-identity-columns — the sixth refusal, measured first (#573)

_2026-08-31T00:00Z_

(Taken from `git hash-object <path>` on the working tree before this
change's first commit and re-taken in the D106 correction round; the
blackbox file itself is not pinned. The
`openspec/changes/refuse-nile-identity-columns/` paths move when the
change archives and are re-pathed then, blobs unchanged unless the D106
round corrects text. Pins die three ways — squash preserves them, an
archive kills the path, a concurrent same-file edit on dev kills the
blob — so every later commit re-verifies all of them, itemized.)




Lead-direct piece under the owner's standing delegation ("continue
processing #282's sub-issues", 2026-08-31, in session). No team; a
one-shot reviewer at freeze. This entry exists because the change alters
what the Nile preset refuses at generate time — a shipped contract — on
the strength of a measurement, and the measurement is the record.

### The measurement

Pinned container
`ghcr.io/niledatabase/testingcontainer@sha256:188a7230d9f39e615bc584d90e8ec6f4754d0ef298701a1d6811d394f3d35696`,
2026-08-31 12:51Z, judgment rule fixed before touching data (Rule 50):
a verbatim server error is a refusal, a verbatim success tag an
acceptance; ambiguity resolves against the claim we would like to ship.

- `create table … (tenant_id uuid not null, id integer generated always
  as identity, name text, primary key (tenant_id, id))` →
  `ERROR:  IDENTITY columns are not supported for tenant-aware table`
- the `generated by default as identity` form → the same error, verbatim
- `create table … (tenant_id uuid not null, name text)` — no primary key
  → `CREATE TABLE`; an insert under `set local nile.tenant_id` succeeds
  and the row is visible under that tenant
- controls: `serial` → `ERROR:  type serial is not supported for
  tenant-aware table` (as previously measured); a uuid composite key →
  `CREATE TABLE` (as previously measured)

Two of the skill's "unmeasured" notes became facts pointing in opposite
directions: identity is refused; a keyless tenant-aware table is
accepted. Column order within a primary key stays unmeasured and is
still not claimed.

### What landed

- `nileIdentityValidator` — either identity kind on a tenant-aware table
  is refused with `nile-identity-in-tenant-table`, evidence grade
  measured-only, the verbatim server error in the message, and a `Next:`
  pointing at a uuid key. Tables without `tenant_id uuid` are untouched
  through the same filter the serial validator uses. It is a separate
  refusal with a separate code, not a widening of the serial one,
  because the platform refuses it with its own error.
- Tests: both kinds refused (one diagnostic each, the column named),
  identity outside a tenant-aware table passes, the keyless test renamed
  from "never measured" to "measured: accepted" with its assertion
  unchanged, the preset's validator count 5 → 6, and the all-refusals
  fixture gained an identity column so the `Next:`-loop test covers the
  new code by construction. Mutant: removing the validator from the
  preset's list turns exactly the five identity-related tests red (two
  refusals, two counts, the loop) and nothing else; restored
  byte-identical.
- `preset-validation`: the Nile requirement's SHALL list and evidence
  paragraph gain the identity refusal and the keyless acceptance; three
  scenarios added, none renamed or dropped (a MODIFIED delta, validated
  strict).
- Skill: a new row in the refusal table (code, evidence, the verbatim
  error) and the paragraph that had carried both "unmeasured" notes now
  states the two measurements; the docs test pins the row's tokens.
- One `minor` changeset for the fixed group.

### D106 gate (after the merge, before the archive)

The isolated spec-only review ran against the dev squash `59a77470`
from a `git archive` export with no `.git` and with `proposal.md`,
`tasks.md`, `blackbox/`, `docs/`, the change archive and the task-time
ledger removed; the evaluator (a fable session, not a team member)
probed the built `@hejbro/nile` nineteen times with every turbo summary
at 0 cached. Verdict **PASS — 0 blocking / 1 major / 6 minor**; the
report ships as `evaluation.md` beside the change and moves with it into
the archive.

The major finding (F1) was the evidence claim itself: the requirement
says both identity kinds are refused and the keyless table is accepted
"as measured", but the Docker-gated live witness — whose header promises
to re-measure every scenario in the file when the image digest moves —
carried neither shape, nor the serial and tenant-less-primary-key claims
inherited from the prior change. The correction round added a fifth
witness: the four measured refusals re-measured as raw DDL against the
pinned digest (serial, tenant-less primary key, identity of each kind
with the platform's verbatim text) and the keyless table accepting a row
under a tenant context. Its first run paid for itself: the inherited
primary-key quotation had dropped the server's own quotes around
`"tenant_id"`, so the witness failed on the verbatim match and the
quotation was corrected in the skill and the validator comment — a
measured claim that no test re-measures drifts exactly this quietly.
The minors were folded into the same round: the
skill's "all three" paragraph now counts four and names the witness (F2);
the primary-key validator's comment no longer calls the keyless shape
unmeasured (F3); the identity message dropped its embedded date and
states a way forward for a non-key counter as well as for a key (F4,
F7); the "outside a tenant-aware table" test binds both identity kinds
(F5); and the doc test asserts the evidence grade on the identity row
itself rather than anywhere in the file (F6). A `patch` changeset
records the message change.

### Method notes

The container probe ran before any artifact was written, with the
outcomes pre-registered as "refused / accepted" per statement — the
identity result matched the serial family's, the keyless result did not
match the cautious expectation, and both were recorded as measured
rather than reasoned about. The piece was sized at agent scale (the
ledger's recent 3–10 minutes per group) and landed inside it.

Migrated from the single-file entry `.blackbox/2026-08-31-refuse-nile-identity-columns.md`, kept verbatim at `.blackbox/573/artifacts/2026-08-31-refuse-nile-identity-columns.md`.

