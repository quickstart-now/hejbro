# Work — quickstart-now/hejbro#695

What was built, measured and reversed under the decisions, one entry per PR or group (`W#`). Managed by `blackbox add work`; append-only.

<a id="w1"></a>
## W1 — Observer pins landed; NB5 restated after #749

_2026-09-05T08:36Z_

NB1 guard on loader.test.ts; NB3 generate half (both edges, byte-identical SQL in either file order); NB4 trigger half (synthesized function passes the authority guard); NB5 is now the refusal, not the rendering — #749/D6 narrowed `ReturnableQuery` after this issue was filed, so update and delete get the same `return-expects-whole-row` pin insert already had; NB6 export dropped, comment restated.

