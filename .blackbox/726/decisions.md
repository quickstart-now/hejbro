# Decisions — quickstart-now/hejbro#726

Every decision on this work item, appended as it is made: owner decisions (`D#`, English rewrites of the owner's words) and AI rulings (`R#`, with kind, basis and ratification). Managed by `blackbox add`; append-only.

<a id="r1"></a>
## R1 — option (b): the column line becomes true by check's inventory line; brownfield witness asserts it (see 707/R1)

_lead · interpretation · basis 412/D12, 412/D13, 412/R28 · 2026-09-04T22:19Z · ratified: pending_

Same ruling as 707/R1 (one change, one rule). For this issue: option (b) — the loss report's column sentence ("check reports this column until it is renamed") becomes true because `check` now names a DB-only column on a managed table as an informational inventory line (Q1 a, Q2); the sentence itself is not edited. The brownfield witness (task 1.8) asserts that promise instead of carrying the `#726` not-asserted comment.

