# Tasks — add-generated-columns

Groups are parallel-safe pieces (no file overlap between groups).
Order: group 1 first (it owns `TMeta`/`columnState`, which groups 2–4
consume), then groups 2 and 3 in parallel (core kinds/snapshot vs
query typing — file-disjoint), then group 4. Every contract detail
(method names, expression form, snapshot fields, version, diff table,
typing rule, witness scope) is owner-settled (D100, 2026-08-28
seven-question trail), so no task carries `[design]`. Estimates
consult `openspec/task-times.csv` (type-assertion tasks 5–8m measured;
integration-harness extension ran 25m pure at ~470-line context —
group 4 sized accordingly).

## 1. Declaration surface (core builders + table())

- [ ] 1.1 (~9m) Builder trio + `TMeta`/`columnState` state. Red:
      `packages/core/test/column-builder.test.ts` — "the three
      generated methods flag meta and columnState (identity kind and
      options recorded; generated records the fragment's node);
      identity methods are integer-family-only at the type level".
      Files: `packages/core/src/types/column-builder.ts`,
      `packages/core/src/types/column-builder-factories.ts` (if the
      chain implementation lives there), their tests.
- [ ] 1.2 (~8m) `table()` misuse validation + implied flags. Red:
      `packages/core/test/not-null-elements.test.ts`-style new
      `packages/core/test/generated-columns.test.ts` — "identity on a
      non-integer column, generated+default, and generated+identity
      each throw a coded error naming the column with a `Next:`
      clause; identity implies notNull and both identity kinds imply
      hasDefault". Files: `packages/core/src/dsl/table.ts`, the new
      test file.

## 2. Snapshot, emit, diff (core kinds) — after group 1

- [ ] 2.1 (~8m) Snapshot v6: column `generated`/`identity` fields
      serialize + deserialize through the codec, version constant
      5 → 6, fixture sweep. Red: `packages/core/test/` snapshot
      round-trip — "generated expression text, identity kind, and
      explicit options survive snapshot → read-back with an empty
      diff; version field reads 6". Files:
      `packages/core/src/snapshot/snapshot.ts`,
      `packages/core/src/kinds/table-snapshot.ts`, snapshot fixtures.
- [ ] 2.2 (~8m) Create-table emit. Red: table-kind emit test + a new
      golden — "the three variants render `generated always as (…)
      stored` / `generated always as identity` / `generated by
      default as identity (start with 1000)` exactly". Files:
      `packages/core/src/kinds/table-kind-emit-sql.ts`, emit tests,
      golden.
- [ ] 2.3 (~9m) Identity diff paths. Red: table-kind diff test —
      "absent↔present, kind change, and each option change render the
      exact `alter column` statements; a live-table golden step
      covers add + re-kind + drop". Files:
      `packages/core/src/kinds/table-kind.ts` /
      `table-kind-emit.ts`, diff tests, golden step.
- [ ] 2.4 (~8m) Generated diff paths. Red: diff test — "expression
      change renders drop+add without the destructive confirmation;
      plain→generated renders drop+add WITH it; generated→plain
      renders `drop expression`". Files: same kind files as 2.3
      (group-internal overlap only), tests, golden step.

## 3. Write-side typing (query) — after group 1, parallel with 2

- [ ] 3.1 (~8m) Excluded-keys classification in
      `InsertInput`/`UpdateInput`. Red:
      `packages/query/test/types/insert-input.test.ts` — "an ALWAYS
      column's key is absent from both input types
      (`@ts-expect-error` on supplying it); a by-default identity is
      optional and writable; chain surfaces inherit via the #351
      wiring (one chain-level probe)". Files:
      `packages/query/src/types/insert-input.ts`, its tests,
      `packages/query/test/types/chain-mutation-input.test.ts`.

## 4. Real-server witness (pg integration) — after groups 1–3

- [ ] 4.1 (~15m) Integration columns: one identity, one generated.
      Red: extend `packages/pg/test/integration.test.ts` — "the
      created table carries the generated grammar (create succeeds =
      PG17 accepts it), identity values arrive assigned, the computed
      column arrives computed, and a deliberate cast-bypassed write
      to an ALWAYS column is rejected by the DATABASE (SQLSTATE
      asserted); the type-level rejection itself is pinned with
      `@ts-expect-error`". Files: that file only.
