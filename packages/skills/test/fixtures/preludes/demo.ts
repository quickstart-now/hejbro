// Meta-test fixture only (packages/skills/test/snippet-check-negative.test.ts) — proves `prelude=` inlines this file into the same compiled unit as the snippet that references it.
export const demoValue: number = 1;
