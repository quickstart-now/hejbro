# hejbro — Implementation Roadmap

Date: 2026-08-19
Prerequisite reading: `/docs/specs/2026-08-19-hejbro-design.md` (the spec).

This roadmap decomposes the approved v1 scope into phases. **Each phase is its
own cycle**: brainstorm remaining unknowns if any → write a detailed
implementation plan (superpowers `writing-plans`) → implement with TDD →
review → PR. Do not start a phase by writing code.

Phases are ordered by dependency, not importance. Phase boundaries are also
good PR boundaries (one phase may still span several PRs).

## Phase 0 — Repo infrastructure ✅ (done at scaffold time)

Monorepo (pnpm + turbo), Biome, commitlint + husky, package skeletons
(`@hejbro/core`, `hejbro`, `@hejbro/supabase`, `@hejbro/skills`), MIT license,
spec + this roadmap. Remaining infra (CI workflow, vitest, build tooling
decision — tsup vs tsc) lands in Phase 1 when there is code to run.

## Phase 1 — Core object model + snapshot + diff for structural kinds ✅

Landed: the `ObjectKind` extension interface (§4.1) and registry; the
schema, enum, and table built-in kinds (serialize/identify/diff/emit); a
deterministic snapshot pipeline and diff engine; the declaration DSL
(`schema`/`pgEnum`/`table` + ~25 column types); migration banner/filename
rendering and the `generateMigration` pipeline; and a golden-file test
harness with a project-specific `posts`/`comments` acceptance case proving
determinism. See issue #2 for the tracking issue and PR history.

The "object kind" plugin interface (§4.1 of the spec) and its first
consumers: **schemas, tables/columns, enums, indexes, FKs**.

- Declaration model + normalized snapshot JSON format (versioned from day 1)
- Diff engine over kinds; `alter table` emission; rename detection question
  deferred to CLI phase (emit both candidates behind an explicit choice)
- Golden-file test harness (this phase sets the testing pattern)
- Add vitest + CI (`pnpm check`, `check-types`, `test`)
- Acceptance: declare the project-specific `posts` table family in a test, generate
  SQL from empty → full and between edits; golden files prove determinism
- Brainstorm resolved (2026-08-19): explicit 4-stage kind contract with
  shared diff helpers extracted once duplication appears ("A+C"); snapshot
  v1 = flat `kind:identity` map, materialized defaults, structured type
  nodes, ordered column arrays (order changes are not diffs); full common
  Postgres column type coverage in this phase; tsdown + ESM-only (D13);
  migration filename rule fixed now, file I/O in Phase 5 (D14); CI on
  Node 22 (D13 amended 2026-08-19). Implementation plan:
  `docs/plans/2026-08-19-phase1-implementation.md`

## Phase 2 — Expression & query builder foundation ✅

Landed: the typed expression AST (`nodeKind`-tagged union covering
literals, comparisons, logical/`in`/`between`, function calls, `sql`
templates, `sql.raw`, and `exists`) with injection-safe rendering
(`renderExpr`) and correlated-subquery scope validation; the operator
library (`eq`/`ne`/`gt`.../`inArray`/`between`/`and`/`or`/`not`/`now()`/
`genRandomUuid()`/`coalesce()`); the drizzle-style `table()` surface with
top-level typed `ColumnRef`s and symbol-hidden metadata (D15); the
`select`/`insert`/`update`/`deleteFrom` type-state query builders with
`where`/`returning`/`onConflict`/`orderBy`/`limit`; expression-based column
defaults replacing `ColumnDefault`, with the snapshot format bumped to v2
to store defaults as rendered SQL (D16); and a golden expression corpus
(`packages/core/test/expr/corpus.expected.txt`) exercising every operator,
injection-string literals, the `sql` template, the project-specific RLS shapes
(using/with-check, correlated `exists`, `exists` + inner join), and the
spec §5.2 function-body query shapes. See issue #3 for the tracking issue
and PR history.

Shared expression AST used by *everything downstream*: column defaults,
RLS `using`/`with check` expressions, view bodies, function bodies.

- Typed expression nodes (`eq`, `isNull`, `now()`, operators, literals…)
- Query builders: `select`/`insert`/`update`/`delete` with `where`,
  `returning` — enough to express real RLS policies and function bodies,
  not a full ORM
- SQL emission with correct quoting/escaping (security-sensitive: this is
  where injection-by-construction is prevented)
- Acceptance: golden files for a representative expression corpus
- Brainstorm resolved (2026-08-19, owner-approved): drizzle-style table
  objects with symbol-hidden metadata (D15); `ColumnDefault` fully replaced
  by the expression AST, snapshot v1 → v2 (D16); phantom typing by Postgres
  type family (D17); `sql` template always quotes interpolations, raw text
  only via `sql.raw()` (D18); expanded standard-clause scope — `order by`,
  `limit`, `on conflict`, `inArray` — and `with check`-shaped expressions in
  the golden corpus (D19). AST is a tagged union with `nodeKind` +
  `assertNever`; literal auto-lift covers string/number/boolean/null/Date
  only; drizzle-inherited operator naming; type-state builders; snapshots
  store expressions as rendered SQL strings. Implementation plan:
  `docs/plans/2026-08-19-phase2-implementation.md`

## Phase 3 — Function/trigger builder DSL + plpgsql compiler ✅

The novelty. Builder execution tracing per spec §5.2/§6.

Landed: a `plpgsqlRef` expression node (unquoted plpgsql-local references —
`new`/`old` fields, function args, locals — under the dual quoting policy)
and `renderSelectInto`, extending the Phase 2 expression/query layer without
touching its invariants; `defineFunction`/`defineTrigger` with a recording
`BodyContext` (`ctx.row`/`ctx.rowOrNull`/`ctx.if`(`.elseIf`/`.else`)/
`ctx.raise`/`ctx.return`) that declares one scalar local per projected
column (`${rowName}_${snake(key)}`, auto-counted when unnamed) and builds a
JSON-safe `FunctionBody` tree; a double-execution determinism guard — the
body callback runs twice at declaration time and a structural (`stableJson`)
mismatch between the two recorded trees is a hard `nondeterministic-body`
error — plus best-effort `declaredAt` capture (`captureDeclarationSite`,
pure stack parsing, no I/O) threaded through every declaration- and
recording-time error; a deterministic plpgsql emitter (`renderFunctionSql`/
`renderTriggerSql`) and the `function`/`trigger` object kinds registered in
the default registry (functions `create or replace` on an identical
structured signature, else a `drop function` + `create or replace function`
statement pair; triggers always recreate via an idempotent
`drop trigger if exists` + `create trigger` statement pair — both recreate
paths render from a **single `alter` `KindChange`**, not two separate
changes, so the diff engine's global create-before-drop ordering can't
split the pair; a `defineTrigger` input expands to
`[functionDeclaration, triggerDeclaration]` in `generateMigration`); and the
`comments-single-depth` golden acceptance case, a 1:1 port of the original
production schema's hand-written trigger, manually verified semantically equivalent to the
original SQL. The acceptance golden's human review caught a real
engine-level bug this way (#55, fixed in PR #56): a same-identity recreate
represented as two separate `KindChange`s let the diff engine hoist the
create ahead of the drop, so the recorded migration dropped the trigger it
had just created — no prior test exercised a recreate through the full
`generateMigration` pipeline. See issue #4 for the tracking issue and PRs
#51–#58 for the implementer/reviewer cycle.

- `defineFunction` / `defineTrigger`; `ctx.row`, `ctx.if`, `ctx.return`,
  `ctx.raise`, `ctx.forEach`, variable declaration; recording proxies for
  row/column refs
- **Double-execution determinism guard** (hard error on differing trees)
- plpgsql emitter; signature-change detection → drop+create vs replace
- Ported the original production trigger `comments_single_depth`
  (the canonical motivating example: `if` + `raise` + row select). Loops
  (`ctx.forEach`) landed in-phase via PR #58 — decision A9 originally
  deferred them to a follow-up sub-issue (#50), which the owner then
  approved for in-phase landing once the rest of Phase 3 was done
- Acceptance: the comments-single-depth trigger ports 1:1 and the emitted
  SQL is semantically equivalent to the hand-written original (manually
  reviewed against the source migration)
- Brainstorm resolved (2026-08-19, owner-approved via main): `plpgsqlRef`
  node + dual quoting policy (A1/A3); scalar-per-column row locals over a
  `record` variable, strictness in the method name (A2); structural
  double-run determinism guard, declaration-site best-effort capture (A4,
  A8); function `create or replace` only on identical structured signature,
  else drop+create (A5); trigger always-recreate (A6); `ctx.raise`/
  `ctx.return`/`ctx.if` shape fixed for this phase (A10); `ctx.forEach`
  originally deferred (A9) then approved for in-phase landing (#50, PR #58)
  — its loop variable is a single `record` local, unlike `ctx.row`'s
  scalar-per-column strategy, since a `for … in` variable is reassigned
  fresh every iteration; function snapshot stores rendered `bodySql`
  alongside
  `bodyHash` since `ObjectKind.emit` renders from snapshot nodes only (A11,
  escalated to and approved by the owner mid-phase). Implementation plan:
  `docs/plans/2026-08-19-phase3-implementation.md`. Decision log entries
  D20–D24 (spec §3) record the phase's brainstorm outcomes in full.

## Phase 4 — RLS, views, grants kinds ✅

Security and read surface next to tables: policies, views, grants (spec
§5.4/§6.5).

Landed: two new declaration surfaces and four new object kinds, all built
on the Phase 1–3 `ObjectKind` contract without touching `tableKind`'s
snapshot shape (`HEJBRO_SNAPSHOT_VERSION` stays 2). `rls.policy(name)` is
a type-state builder chain — `for()` exposes only the clauses each
Postgres command allows (select/delete → `using`, insert → `withCheck`,
update/all → both, `.to()` mandatory) — and `TableExtras.rls` binds an
`RlsInput` to its owning table via `bindRls`, validating duplicate policy
names, illegal clause/command combinations, and top-level foreign-column
references (a correlated `exists()` is exempt — `collectColumnRefs`
deliberately doesn't descend into it). The `rls` kind (identity
`<schema>.<table>`) emits enable/force/disable; the `policy` kind
(identity `<schema>.<table>.<name>`) has no `alter policy` in Postgres, so
any change recreates via a **single** `alter` `KindChange` whose `emit`
orders drop before create (D23/#55 precedent, first applied outside
triggers/functions here). `defineView` copies an already-validated
`select()` chain's query node in as-is; the `view` kind derives its
column list from the query's projection and applies the **prefix rule**
(D27) — a previous column list that's a prefix of the next can
`create or replace view` in place, anything else (removal, reorder,
rename) recreates via the same single-`alter` pattern. `grant()` is a
type-state chain (`.usage` / `.tables(...)` / `.defaultPrivileges.tables(
...)`, each ending `.to(...roles)`) that fans out into one
`GrantDeclaration` per role (D28); the `grant` kind's identity is
`(schema, grantKind, role)`, so duplicate declarations fall out of
`buildSnapshot`'s existing `duplicate-identity` check with no bespoke
code, and `diff` is a privilege-set delta whose `emit` **recomputes** the
added/removed privileges from the snapshots rather than reading banner
notes — a review fix first applied to `viewKind.emit` (PR #71) and then
built into `grantKind` from the start (PR #72), establishing notes as
strictly display-only across every kind that recreates or deltas.

- `rls.enabled` + policy declarations (using Phase 2 expressions);
  drop+create diff strategy
- `defineView` (create or replace; drop+create on column removal)
- `grant()` declarations; snapshot-delta → `grant`/`revoke` emission
- Acceptance: `app-security` golden case — the original production schema's
  `grants.ts` corpus (1:1 port; its one `to anon, service_role` statement fans out to
  two per-role `grant usage` statements, semantically equivalent) plus a
  representative RLS slice ported from the **legacy** production-schema migrations
  (A6: the current production schema is grants-only, so legacy is the only
  real RLS source available for the port) — `posts_read_published`,
  `post_translations_read_published`, `comments_read_visible` — plus one
  insert/`with check` policy shaped after a planner-supabase migration but
  expressed with core operators only (no `auth.jwt()`, Supabase-preset
  territory — Phase 6, #66). Manually verified clause-by-clause equivalent
  to the source SQL, the same review discipline that caught #55 in Phase
  3.
- Brainstorm resolved (2026-08-19, owner-approved): RLS as two additive
  object kinds rather than embedding into the table snapshot (D25); the
  policy type-state chain and mandatory `.to()` (D26); the view prefix
  rule (D27); grant identity and per-role fan-out (D28). Implementation
  plan: `docs/plans/2026-08-19-phase4-implementation.md`. Decision log
  entries D25–D28 (spec's decision log) record the phase's brainstorm
  outcomes in full. Landed across four PRs against sub-issues of #5: PR
  #70 (#62, rls/policy kinds), PR #71 (#63, view kind), PR #72 (#64, grant
  kind), and this acceptance/close-out work (#65). Typed roles,
  `auth.uid()`/`auth.jwt()` helpers, and RLS-related warnings are
  Supabase-preset work tracked as a Phase 6 follow-up (#66).

## Phase 5 — CLI (`hejbro init` / `hejbro generate` / `hejbro verify`) ✅

The pure Phase 1–4 core meets the filesystem and the clock for the first
time — `packages/cli` (the `hejbro` package) is the *only* place either
happens; `@hejbro/core` gained one new pure engine (`planRenames`,
`checkChain`) and stayed dependency-free throughout.

Landed: `hejbro.config.ts` loads through **jiti** (D29) — the same path
declaration entries use, exercising the `hejbro` package's own
self-import cycle (`import { defineConfig } from "hejbro"` from inside a
file jiti loads) — and is validated by a `defineConfig()` + zod schema
whose issues are re-wrapped into spec §7 diagnostic grammar; zod's own
error text never reaches the user (D30). Table/column/index/FK names
are validated against `^[a-z][a-z0-9_]*$` at declaration time (D36),
the strictness the `--rename`/`--confirm-drop` flag grammar's `.`/`=`
separators require. `hejbro generate` diffs the rebuilt-from-
declarations snapshot against the checked-in one, resolves any
same-table/-schema drop+add ambiguity via core's new `planRenames`
(rule A: type-agnostic, batch-collected, residual-set-aware so
partially-resolved reruns narrow correctly) against repeatable
`--rename`/`--confirm-drop` flags (D32) — every ambiguity renders as
the owner-approved rich terminal diagnostic (identity, body, and
concrete rerun-command suggestions built from `planRenames`'
*structured* residual data, not a re-parse of its flat message text) —
and, on success, writes the migration with the existing banner format
(spec §6.6) plus two new `parent-snapshot:`/`snapshot:` sha256 lines
(D33) that chain migrations together. `hejbro verify` reconstructs that
chain purely from the checked-out files (`checkChain`, no live DB) and
runs a dependency-aware batch of four checks — snapshot parses; chain
linearity (both always run, independent of each other); declarations ↔
snapshot (needs a parsed snapshot); chain tip ↔ snapshot (needs both
priors) — reporting every failure in one multi-diagnostic batch with a
`skipped:` line for any check a failed dependency ruled out, not just
the first failure (D34, redesigned mid-phase from the original
sequential-short-circuit reading once reviewer confirmed the batch path
was cheap to reuse from PR B rather than new infrastructure). `hejbro
init` idempotently scaffolds the config, migrations directory, and an
empty snapshot, reporting skips and always exiting 0 (D35) — the
missing-`entry`-file case doubles as the onboarding surface, its error
carrying a complete example. `examples/cli-smoke` proves the whole loop
end to end against the *built* CLI binary in a tmp copy: init →
generate → generate (no-op) → verify → a column rename that's
ambiguous until resolved by the suggested `--rename` flag → verify
again.

- `hejbro.config.ts` via `defineConfig()`, zod-validated (entry files,
  migrations dir, snapshot path, prefix strategy) (D30)
- TS declaration + config loading through **jiti only** (D29) — the one
  place core purity meets the filesystem
- `hejbro generate`: migration file emission with the banner comment format
  (spec §6.6) plus the new `parent-snapshot:`/`snapshot:` sha256 hash lines
  (D33)
- Non-interactive rename/drop confirmation via repeatable
  `--rename <schema>.<table>.<old>=<new>` / `--confirm-drop
  <schema>.<table>.<column>` flags (rule A; no TTY prompts) (D32)
- `hejbro verify`: four-check snapshot/chain consistency command for CI
  (snapshot parses, declarations ↔ snapshot, chain linearity, chain tip ↔
  current snapshot) (D34)
- `hejbro init`: idempotent scaffold of config + migrations dir + empty
  snapshot (D35)
- Strict SQL identifier validation (`^[a-z][a-z0-9_]*$`) at declaration time,
  required by the rename flag grammar (D36)
- Acceptance: `examples/` project generates end-to-end from the CLI
  (`init` → `generate` → `verify`, including a rename round-trip)
- Brainstorm resolved (2026-08-20, owner-approved): jiti-only loading
  (D29), zod-validated config (D30), citty as the CLI framework (D31),
  flag-based non-interactive renames with rule A (D32), the single
  snapshot file's banner hash chain (D33), `hejbro verify`'s four checks
  (D34), `hejbro init`'s idempotent scaffold (D35), and strict SQL
  identifier validation (D36). Implementation plan:
  `docs/plans/2026-08-20-phase5-implementation.md`. Decision log entries
  D29–D36 record the phase's brainstorm outcomes in full. Landed across
  five PRs against sub-issues of #6: PR #79 (#74, core rename-plan
  engine + identifier validation + banner hashes), PR #80 (#75, CLI
  package foundation + config + loader + diagnostics + `init`), PR #81
  (#76, `generate` command + golden error corpus + golden help output),
  PR #82 (#77, `checkChain` + `verify`, including the mid-phase batch
  -reporting redesign), and this acceptance/close-out work (#78).
  Deferred: an onboarding example schema page, the full rename-rules
  walkthrough, a `hejbro verify` CI example, the `Next:`-marker
  retrofit onto Phase 1–4 errors, `assertSqlName` for explicit index
  names, the `--flag=value` equals-joined token form, and an npm-pack
  install-path smoke test — tracked as Phase 7/8 issues (see below).

## Phase 6 — Supabase preset (`@hejbro/supabase`) ✅

Built strictly on the provider interface — this phase was also the
interface's first real test (spec §4.1: if it needs a core special case, fix
core).

Landed: two pure core additions that grow the public extension interface
from four to five preset contributions (D37), landed *before* any Supabase
code, then `@hejbro/supabase` built entirely on that public surface. A
**validation channel** — `generateMigration({ validators })` runs
preset-supplied pure functions over the built snapshot and normalized
declarations, returning `warnings`; error-severity diagnostics join `errors`
and short-circuit generation exactly like rename errors (D37). An
**`existingTable()` primitive** — a reference-only `Table` usable as an FK
target, in `exists()`, and in view from/joins, but never declared, diffed,
or emitted; passing one to `generateMigration` is the hard error
`existing-table-declared` (D41). Core also gained a branded `Role` type
(D43, non-breaking — `grant().to()`/`rls.policy().to()` widened to
`string | Role`). On top of that surface, `@hejbro/supabase` landed role
presets (`anonRole`/`authenticatedRole`/`serviceRole`), the
`authUid()`/`authJwt()` expression helpers (D45 — plain `auth.uid()`/
`auth.jwt()` calls, no automatic initPlan wrapping), the prebuilt
`authUsers` existing-table reference, three validators (reserved-schema
hard error covering `auth`/`storage`/`realtime`, D38; exposed-table-
without-RLS warning, D40; view-over-RLS-without-`security_invoker`
warning, #66/D39 — `defineView`'s JSDoc now points here instead of
carrying the TODO), and the storage bucket object kind
(`supabase-storage-bucket`, the first row-data kind: `create`/`alter` emit
one idempotent `insert … on conflict (id) do update set …` into
`storage.buckets`; `drop` emits no SQL, and its manual-deletion guidance
now surfaces in the migration banner too — `bannerLabel`'s `drop` case
gained the same `notes.join(", ")` rendering `alter` already had,
additive since no existing drop change ever carried notes, D42). Two
acceptance examples close the phase: `examples/preset-smoke`, a toy
zero-Supabase custom kind (`smoke-schema-note`) plus one expression helper
(`txidCurrent()`) built on `@hejbro/core` alone — proving the extension
interface is generic by construction, not by claim — and a **reduced**
example (later renamed `examples/supabase`, D53) that exercises every
preset feature exactly once
(`authUsers` FK, `authUid()` RLS policy, storage bucket, role-preset
grant, the exposed-table warning, and the #66 warning, plus a negative
`schema("auth")` case) through `generateMigration({ registry:
<default + registerSupabaseKinds>, validators: supabaseValidators })`,
golden-asserted end to end (D44 — the full production-schema port stays Phase 7).

- Role presets (`anon`, `authenticated`, `service_role`)
- `auth.uid()` typed helper
- Storage bucket object kind
- Reserved-schema protection (`auth`, `storage`, `realtime`)
- "Exposed table without RLS" warning
- Acceptance: the reduced example uses the preset; a second toy preset stub
  (e.g. `examples/preset-smoke`) proves the interface is generic
- Brainstorm resolved (2026-08-20, owner-approved): the validation channel
  and `existingTable()` core additions (D37, D41), reserved-schema as a
  hard error (D38), the exposed-table condition (D40), #66's
  declaration-side judging (D39), the storage bucket row-data kind (D42),
  the branded `Role` type (D43), plain (unwrapped) `auth.uid()` (D45), and
  the reduced two-example acceptance split (D44). Implementation plan:
  `docs/plans/2026-08-20-phase6-implementation.md`. Decision log entries
  D37–D45 (spec's decision log) record the phase's brainstorm outcomes in
  full.
- **§4.1's first real test, passed.** Every `packages/supabase` source and
  test file imports only from `@hejbro/core`'s public `index.ts` — zero
  deep imports, verified by grep at every PR in the phase. More
  precisely: the preset itself required **zero** core special cases (no
  existing core kind or emitter ever branches on a Supabase concept) — but
  building it *did* surface that the interface itself was one contribution
  short (a cross-kind validation hook) and one primitive short (a
  reference-only external-table type). D37 and D41 are exactly the "fix
  the interface" half of §4.1's promise: two small, generic, non-breaking
  widenings of the *public* surface, landed before any Supabase-specific
  code, then reused as-is by a toy preset (`preset-smoke`) that has never
  heard of Supabase. That reuse is the proof the widening was generic and
  not a special case in disguise.
- Landed across five PRs against sub-issues of #7: PR #98 (#91, core
  validation channel), PR #99 (#92, core `existingTable` + branded
  `Role`), PR #100 (#93, preset foundation — roles, auth helpers,
  `authUsers`, the three validators; also closed #66), PR #101 (#94,
  storage bucket kind, plus a same-PR core fix rendering `drop` notes in
  the migration banner, reviewer-flagged and owner-approved), and this
  acceptance/close-out work (#95).
- Deferred: CLI preset wiring (a `presets` config field plus warning
  rendering in `hejbro generate`'s output — D30's "no preset fields
  reserved" decision stays untouched until then; reopened by D55 in Phase
  7) and `authUid()`'s initPlan-cached variant (D45) were filed as Phase 7
  follow-ups during the phase brainstorm (#96, #97; #97 later moved to
  Phase 8 by D47). A rare (~1 in 14 observed) CLI e2e test flake surfaced
  while re-running this phase's clean-state gates — the built CLI binary
  occasionally isn't resolved on a cold `turbo run test --force`; not
  reproduced on repeat runs, not caused by this phase's changes (isolated
  to a pure string-formatting change unrelated to process spawning or
  tmp-dir I/O) — tracked as a Phase 7 follow-up (#102) alongside the CI
  hardening `examples/cli-smoke` already exercises. D44's "full port"
  acceptance bar for Phase 7 was superseded at the Phase 7 brainstorm by
  D53 (generic `examples/postgres` / `examples/supabase`).

## Phase 7 — Table-kind completeness, local round-trip examples, skills, README ✅

Brainstorm resolved (2026-08-20, owner-approved): decision log entries
D46–D56, plus D57 (naming unification, decided 2026-08-21). Two roadmap changes came out of it: the round-trip runs
**locally via Docker**, not as a CI job (D49), and the showcase examples
are **generic and named by database** — `examples/postgres` and
`examples/supabase` — not a port of any particular project (D53).
Implementation plan: `docs/plans/2026-08-20-phase7-implementation.md`
(written after the owner reviews this section and the spec rows).

Work groups, in PR order:

1. **Table-kind completeness** (D46, D50–D52; #104, #105, #106, #22) —
   CHECK constraints (`check(name, expr)`), partial indexes
   (`.on(...).where(expr)`) and ordered index columns (`desc(t.col,
   { nulls })`), FK `on update` (+ `set default`), self-referencing FKs via
   the callback's own column refs (`references: { columns: [t.id] }`,
   `table` optional everywhere). **Snapshot version 2 → 3** (index columns
   become objects; all goldens and banner hashes regenerate, no shim). In
   scope: the `emitAlter` omission of changed indexes (drop + create) and
   `duplicate-index-name` / `duplicate-foreign-key-name` hard errors. #88
   rode along (index(name) validates through assertSqlName, PR A2).
2. **`examples/postgres` + local round-trip script** (D48, D49, D53; #107)
   — core-only showcase: tables, CHECK, partial/ordered indexes, a
   self-FK, RLS, a trigger, grants, a view, with a designed 4-step
   migration chain (baseline → add column + CHECK → change FK actions →
   move a column across tables). `pnpm roundtrip` on `postgres:17-alpine`
   via the Docker CLI only: chain-applied DB vs fresh-migration DB,
   container `pg_dump` both, diff must be empty. `ci.yml` unchanged;
   `pnpm test` stays DB-free.
3. **`Preset` type + config `presets`** (D55; #96) — core `Preset` data
   object, `supabasePreset`, `hejbro.config.ts` `presets: []`, `generate`
   /`verify` register preset kinds, `generate` renders `warnings` to
   stderr (§7 grammar; golden texts owner-approved).
4. **`examples/supabase`** (D53, D55; #107, #96) — preset showcase seeded
   from the reduced Phase 6 example: role presets, `authUsers`,
   `authUid()`, a storage bucket, validators/warnings; `hejbro.config.ts`
   + CLI e2e mirroring `cli-smoke`; seeded round-trip (roles + a
   `storage.buckets` stub) including the bucket row query.
5. **`@hejbro/skills`** (D54; #108) — one `hejbro` skill installed with
   `npx skills add quickstart-now/hejbro`: short `SKILL.md` + four
   references (dsl-cheatsheet, function-builder-pitfalls,
   generate-verify-workflow, supabase-preset); samples link to the two
   examples, a vitest asserts every linked path exists.
6. **README landing page + `docs/guide/`** (D56; #109 with #83, #84,
   #85) — pitch → 60-second example → how it works → packages/examples →
   agents → built AI-natively → status → license; **no comparison
   table**. `docs/guide/getting-started.md`, `renames.md`, `ci.md`
   (markdown only, no Pages site). Repository-wide sweep replacing the
   earlier project-specific example naming with the generic examples
   (roadmap, spec, README, `examples/README.md`, Phase 3 golden
   comments).
7. **#27** — warn when adding a not-null column without a default (the
   class an empty-database round-trip cannot catch); validators channel.
8. **#102** — CLI e2e flake: instrument first (assert the built binary
   exists, keep child stderr), then address the leading suspect (shared
   jiti disk cache across parallel workers). No longer a precondition for
   anything (D49).
9. **Naming unification** (D57; #128) — snapshot vocabulary
   (`schemaName`→`name`, `functionName`→`function`) and kebab-case for
   artifact tokens (`GrantKind`'s three values). The snapshot's own version
   field, `hejbroSnapshot`→`formatVersion`, is a format change in its own
   right — **snapshot version 3 → 4**, with `parseSnapshot` still
   recognizing the old key on a pre-v4 file so it gets the normal "older"
   message. `packages/core/test/naming-conventions.test.ts` scans generated
   output (not source text) for the vocabulary/kebab rules. Goldens and
   both examples' migration chains regenerate; both round-trips re-run
   green.

- Acceptance met: both examples' local round-trip green — `examples/postgres`
  `round-trip OK: 128 dump lines identical`, `examples/supabase`
  `round-trip OK: 66 dump lines identical` plus a matching `storage.buckets`
  row (row data is invisible to `pg_dump --schema-only`, so it is compared by
  query, D48). `pnpm check` / `check-types` / `test` green throughout.
- Landed across twelve PRs against sub-issues of #8: PR #111 (#104, #106,
  #22 — CHECK constraints, FK `on update`, self-referencing FKs), PR #112
  (#105, #88 — ordered and partial indexes, snapshot v3), PR #114 (#96 —
  `presets` in `hejbro.config.ts` and warning rendering, reopening D30),
  PR #115 (#27 — the not-null-without-default warning), PR #117 (#102 —
  hardened e2e harness, jiti fs cache disabled), PR #123 (#122 — emit
  dependent-object drops before table alters), PR #124 and PR #126 (#107 —
  the `examples/postgres` and `examples/supabase` showcases with their
  local round-trip scripts), PR #127 (#108 — the `hejbro` agent skill),
  PR #119 (#118 — project-specific naming replaced in tests and goldens),
  PR #133 (#128 — the D57 naming unification and the v4 snapshot format),
  and PR #134 (#109, with #83, #84, #85 — the README landing page and
  `docs/guide/` pages).
- Decisions: D46–D56 from the phase brainstorm, plus **D57** decided
  mid-phase (2026-08-21) once two vocabularies had visibly diverged.
- **The local round-trip earned its keep on day one.** Running real SQL
  against real Postgres surfaced six defects that goldens cannot see,
  because a golden only proves the generator is self-consistent: the role
  precondition (roles are cluster-level and hejbro never creates them, so
  both examples seed theirs), one-shot schema grants (#121, fixed in both
  examples with `defaultPrivileges`), a genuine engine bug emitting
  dependent-object drops after table alters (#122 → PR #123), a harness
  that skipped its own comparison and still reported success, a missing
  `auth.uid()` seed stub, and a single-package symlink that crashed the
  CLI (#125). A seventh defect, #129, came from an owner thought
  experiment rather than execution: `verify` misreports a linear history
  as `diverged-migrations` when the chain returns to a snapshot state it
  held before.
- Carried into Phase 8 (#9): #23, #24, #25, #26, #87, #89, #97 (moved by
  D47), plus #110, #113, #116, #120, #121, #125, #129, #130, #131, #132
  filed during the phase.

Next: **Phase 8 (#9)** — release readiness.

## Phase 8 — Release readiness (owner-gated)

Brainstorm resolved (2026-08-21, owner-approved): decision log entries
D58–D68, plus an in-place amendment to D33. D69 and D70 were added at
plan review. Two things came out of the brainstorm that change how the
phase is scoped.

**The judgement default was reversed (D65).** The phase began by asking
"is this safe to defer?" and answering it with "does this change rendered
output?". Three of those answers were then shown wrong by research in the
same brainstorm: `serial` was emitting SQL Postgres rejects outright,
#24 concealed a primary key that is silently dropped when a PK column is
added, and both of #110's cheaper options turn out to be breaking after
publication. The question is now **"is there an active reason not to do
this now?"**, and the axis is **known defect or new feature**, not
rendered output. D16 had already recorded the underlying rule —
*pre-publication is the only cheap moment* — and D51 and D57 each used it;
D65 states it and widens it.

**The snapshot format moves to 5 (D68).** #110(b) stores expressions as
structured nodes and #24(iii) records pk/unique constraint names, both of
which change how an unchanged declaration renders. D33 is amended to allow
the structural expression nodes. #23's new `sequence` kind needs no bump of
its own (Phase 6 added `supabase-storage-bucket` without one) but lands in
the same wave.

Work groups, in rough PR order:

1. **Written plan** (this section, the D58–D70 rows, the AGENTS.md hard-gate
   change from D63, and `docs/plans/2026-08-21-phase8-implementation.md`).
2. **Release infrastructure** (D58, D59, D62, D63; #86, #28) — the
   pack-install smoke test and the packaging it proves (LICENSE per
   package, a README for `hejbro`, `homepage`/`bugs`/`keywords`,
   `prepack`), `engines` plus a Node 22 CI matrix entry, `@hejbro/skills`
   set `private`, Changesets with the three published packages in a fixed
   group, and the two release workflows split by branch.
3. **`scripts/regen-examples.sh`** — the example chains are regenerated by
   driving the built CLI once per step, which is manual today. Every
   format change and every new chain step needs it, so it lands before the
   format wave and enumerates the step files rather than hard-coding how
   many there are.
4. **Diagnostics and CLI** (#25 → #125, #129, #89, #138, #87, #137's
   guard) — none of these touch rendered snapshot output. #25 comes before
   #125 because making `HejbroError` a real `Error` subclass removes the
   duck-typing that misclassifies Node's `ERR_MODULE_NOT_FOUND` as a
   hejbro error in the first place.
5. **Snapshot v5** (D66, D67, D68; #136, #26, #110, #23, #24, #137) — the
   version bump and the version-mismatch message together, then the
   structured expressions, then the `sequence` kind with its rename drift
   guard, then constraint names in the snapshot with the pk/unique alter
   emission that needs them.

   **#157** is a fifth pre-rendered field that surfaced after the
   brainstorm: `ViewSnapshot.selectSql`, found while implementing #110 and
   reusing the same codec. It is **not** a defect fix — `create or
   replace view` already handles a rename correctly — but after
   publication it cannot change without a v6. Recorded as **D72**; v5
   carries it under D68's single pre-publication bump.
6. **Correctness and content** (#121, #120, #113, #116, #97) — the
   schema-wide grant fix, the golden trigger messages translated to
   English, the RLS predicate widening that lets the showcase drop its
   `isNotNull(t.id)` workaround, the storage-bucket banner notes, and the
   initPlan-cached `authUid()` variant with the skill, README and example
   text that currently teach the uncached form.
7. **The preset checked against the real platform** (D69) — a second
   local-Docker script running `examples/supabase` against a pinned
   `supabase/postgres` image, after the preset changes above and before
   the docs. Today that example runs on plain Postgres against a role and
   a `storage.buckets` table **we wrote ourselves**, so it verifies our
   assumptions against our assumptions; the round-trip cannot catch an
   error both of its sides make, which is how `serial` survived two
   phases. Any mismatch found here is a new defect, filed as an issue.
8. **Coverage gating** (#154) — coverage had never been measured in this
   repository; measuring it once surfaced a real defect class (the nested
   expression walkers had never recursed, absorbed by #110). The owner
   decided to gate on the CRAP score (`CC² × (1 − coverage)³ + CC`) for
   `@hejbro/core` and `@hejbro/supabase`. The CLI is out of scope: its
   commands are exercised end-to-end through a spawned subprocess, which
   in-process V8 coverage cannot observe, so its number is a measurement
   artifact rather than a gap. Measured on `dev` across 522 named
   functions:

   | Threshold | Violations | Effective cap |
   |----------:|-----------:|---------------|
   | 30 | 3 | CC ≤ 29 |
   | **10** | **17** | CC ≤ 9 |
   | **5** | **46** | CC ≤ 4 |
   | 4 | 69 | CC ≤ 3 |

   Five PRs: a reporting script, the 13 functions at complexity ≥ 10
   (where no amount of coverage clears the threshold, so they need
   splitting rather than tests), the 4 at complexity 8–9 (which clear at
   70–77%), then **the gate at 10 (D71)**, and then **a ratchet to 5
   within the same phase**. At 100% coverage `CRAP = CC`, so each
   threshold is in effect a cap on cyclomatic complexity. The ratchet is
   the one item in track A with **no pre-publication deadline of its
   own** — recorded so that schedule pressure, if it comes, is not aimed
   at the format items, which cannot move. **Landed 2026-08-22 at
   `dev@b2776c4`** (#154 closed): the gate went in at 10 (#233, D71),
   then the ratchet — remeasured at 42 functions, not the 29 this plan
   once said, and split into seven behaviour-preserving PRs (#241, #242,
   #247, #249, #252, #253, #254), each with a red-first proof per
   function — and the flip to `CRAP_THRESHOLD = 5` was the last commit of
   the last one; CI now runs the gate at 5 and reports 0 of 924 functions.
9. **Docs and release** — README status and install instructions,
   `CONTRIBUTING.md` (the changeset rule, the release checklist, the local
   round-trip gate, and the warning that merging the version PR publishes
   irreversibly), then the 0.1.0 release itself.

**The round-trip becomes a regression line, not just a showcase.** Two of
the defects found this phase are chain-vs-fresh asymmetries — #137's drop
path (removing a column from a composite primary key leaves the chain
database with no primary key at all) and #121 (a schema-wide grant never
reaches tables added later) — and the round-trip is exactly the instrument
that catches those. It did not, because the example chains contain neither
a primary-key change nor a table added under a schema-wide grant. Both
fixes therefore extend the chains, and the rule going in is that **every
step defends at least one defect class**, recorded next to the step so a
later reader can see what deleting it would cost.

Issues filed from the brainstorm's own research, all defects rather than
ideas: **#136** (the snapshot version-mismatch message claims
pre-publication and sends the user in a circle — delete the snapshot, and
`verify` then tells them to restore it from version control), **#137**
(`emitAlter` never maintains the primary key constraint, on either the add
or the drop path), **#138** (`tableMeta` and `triggerRowMeta` use
per-instance `Symbol()`, so two resolved copies of `@hejbro/core` silently
stop recognising each other's tables — the CLI loader's fallback covers
one of the five sites, and the `authUsers` path is not one of them), and
**#139** (the OIDC switch, which cannot be configured until the packages
exist on the registry).

Four of those were at first deferred to 0.2.0 with an active reason —
**#130** (new commands, four design questions open), **#131** (internal
tooling), **#132** (needs a per-path lint design) and **#139** — and on
2026-08-22 the owner decided that **every open sub-issue of #9 is handled
in this phase**. #130 (`history`/`restore`), #131 (source aliases for
in-process tests) and #132 (`useNamingConvention`, per-path) have landed;
**#139 is the one tail that stays open past the release by its nature**:
a trusted publisher can only be configured on a package that already
exists on npm, so the branch (`phase8-oidc-publishing`) is ready and is
applied, with the owner, after 0.1.0 is on the registry. Apart from that
tail the phase's work is done; **the frontier is the release itself**,
which is owner-gated (see the hard gates in `AGENTS.md`) — and was
taken on 2026-08-22 (see the closeout note below).

Three items from the original sketch of this phase changed. D62 settled the
skills question the other way: `@hejbro/skills` gets **no npm channel**,
so the earlier "bundle the skill files into `@hejbro/skills` (D54's npm
half)" no longer applies — the repository stays the only distribution and
the package is `private`. D64 moves the **GitHub Pages docs site out of
this phase** into a follow-up; 0.1.0 ships the README and the
`docs/guide/` markdown, with `homepage` pointing at the repository. The
third — verifying the preset against a real `supabase/postgres` image —
was listed in the sketch but never scheduled, and is now group 7 (D69).
The two scripts coexist because they answer different questions: the
plain-Postgres round-trip proves the core generator is deterministic, the
image run proves the preset matches the platform it claims to target.

**Phase 8 closed 2026-08-22.** 0.1.0 was published the same day
(Version Packages PR #179 merged on `dev` at `e1ae041`, `dev` → `main`
merge commit #257 at `5ee81de`, `release-publish` run 32559742995 with
the owner approving the `npm` environment; `@hejbro/core@0.1.0`,
`hejbro@0.1.0`, `@hejbro/supabase@0.1.0` on the registry with provenance).
#139 landed right after (#259): `release-publish.yml` now publishes via
OIDC trusted publishing, verified at the next release. A hotfix rode
along with the release (#256): a CLI test had the version string
hard-coded, which the Version Packages bump broke — tests now read the
version from `version.ts`. #9 is closed. The "there is no Phase 9" note
in the Phase 8 plan is superseded: the owner opened Phase 9 on
2026-08-22 after the first dogfood pass on the published packages.

## Phase 9 — 0.1.x stabilization (dogfood-driven) 🚧

Phase issue **#260**. Brainstorm resolved 2026-08-22 (owner-approved):
decision log entries **D80–D81**; plan in
`docs/plans/2026-08-22-phase9-implementation.md`.

**Why a phase, not just a fix list.** The first dogfood pass on 0.1.0 —
a plain npm consumer (`pnpm add hejbro @hejbro/supabase`, own
`tsconfig`, strict, no `skipLibCheck`), the README schema, four edits,
the Supabase preset, all applied to a local `supabase/postgres` and then
*executed* — found one runtime defect and six docs/CLI/DSL gaps within
an hour. The defect (#261: a `returns: <table>` function fails at call
time after a mid-declaration `add column`) sits in a blind spot the
in-tree instruments share by construction: D48's round-trip compares
schema dumps and never calls a function, and every showcase chain only
appends columns. So the phase's acceptance is the scenario set itself,
not the list it produced on day one.

**Model (D80).** Scenarios live in the private
`quickstart-now/hejbro-dogfood` repository and consume the *published*
packages; findings are filed here; this repository gains regression tests
and fixes only; fixes ship as `patch` changesets on 0.1.x (owner-gated,
the four touches under the Phase 8 plan's *Owner actions*).
**Acceptance:** the set passes end-to-end — migrations applied and the
result executed (function calls, RLS queries as restricted roles) —
against a published 0.1.x, and one full pass files nothing new.

**Work, in PR order** (sub-issues of #260):

1. **#261** — the column-order defect → **D81**: snapshot `columns`
   order is physical order; `generate` inherits existing order from the
   parent snapshot and appends new columns; `--rename` keeps position;
   `allColumns` resolves at snapshot-build time; `verify`/`restore` apply
   the same rule; no format bump; `patch`. Pure regression tests per
   case (several columns in one run, rename, drop + re-add,
   `--confirm-drop`, cross-table move, function body and view list
   order, `create table` from a snapshot = snapshot order) plus one line
   of docs (guide + skills): a column inserted mid-declaration lands at
   the end in Postgres; hejbro mirrors that.
2. **#269** — DSL: `defineFunction`/`defineTrigger` take the schema as a
   string while `table`/`defineView`/`grant` take the schema object; the
   owner wants the object. The PR decides, with the owner, whether the
   string form stays accepted on 0.1.x (then `patch`) or goes (then the
   next release is 0.2.0).
3. **#262, #263** — docs: `getting-started.md` still says "not yet
   published"; the README 60-seconds example warns
   `rls-unreachable-schema` (add the usage grant).
4. **#264, #265** — CLI: `restore --help` documents no positional;
   `--help`'s command table is broken by `generate`'s multi-line
   description; `restore`'s undo hint omits that the files are staged.
5. **#266** — bootstrap `hejbro-dogfood` with scenario set v1 (the
   day-one pass, plus mid-declaration insert + view, several columns in
   one generate, drop + re-add, `--confirm-drop`).
6. **#267** — evaluate dependabot #258 (TypeScript 5.9 → 7.0).
7. **0.1.1** (owner) — first `patch` release; **#268** verifies the
   first OIDC publish and then deletes the `NPM_TOKEN` secret.
8. Re-run the set on 0.1.1; new findings become sub-issues of #260;
   0.1.2 if needed; the phase closes on a clean pass.

Out of scope: the GitHub Pages site (D64 follow-up), presets beyond
Supabase, anything under *Deferred*. New `minor` capabilities go to a
0.2.0 phase.

**All sub-issues landed 2026-08-22.** #261 (#277, D81), #269 (#273 —
the string form stays accepted on 0.1.x as deprecated, `patch`), #262 +
#263 (#276), #264 + #265 (#275), #267 (TS 7.0.2 not landable: its
Compiler API does not exist yet, so `check:crap` crashes — #258 closed,
pinned at 5.9; #271/#272 ignore the major bump until 7.1), #278 (#280:
README CRAP item + badge, kept current by `check:crap` with a CI drift
check). **0.1.1 published** the same day (Version Packages #274 on `dev`
at `723a633`, `dev` → `main` merge commit #279 at `44c67f8`,
`release-publish` run 32569555027 via OIDC trusted publishing; #268 done,
`NPM_TOKEN` deleted). **Dogfood acceptance met on 0.1.1**: scenario set
v1 (`hejbro-dogfood` PRs #1–#3) — 8 scenarios, first pass on 0.1.0
reproduced #261 in 02 and 03 (03 as the silent same-type mislabel
variant), second pass on 0.1.1 8/8 PASS with 0 warnings and nothing new
filed. **#260 stays open by the owner's decision; the owner decides when
the phase closes.**

Next: owner call on #260 / 0.2.0 planning.

## Phase 10 — ORM direction (#282, in progress)

Process took two turns before settling: GitHub Spec Kit was adopted as
the "what" layer (#285, dev `85f3600`), piloted on one feature, then
reverted in full (#292, dev `16537d5`; D82/D83 permanently retired). The
feature the pilot carried **shipped and stays**:

- **#284 index completeness** — index access method (`using`), operator
  classes, expression indexes (PR #286, dev `7bbdc8b`; D84–D86;
  `docs/guide/indexes.md`; examples round-trip 164 dump lines identical;
  `patch` changeset pending → next release 0.1.2). The Spec Kit
  artifacts (`specs/001-index-completeness/`) were removed by the
  revert; this entry is the shipping record.

Since 2026-08-26 the phase runs on OpenSpec (D87–D88): the frontier and
plan of record live in `openspec/changes/`, not in this document or a
successor roadmap. Owner-settled direction: hejbro grows into a
PostgreSQL/serverless-specialized TypeScript ORM/query builder — one
product, the schema DSL as the single source of truth for both
migrations and query types. The first OpenSpec change is the
ORM-direction spec (#293). This document stays as the historical record
of the 0.1.x line.

## Deferred (explicitly out of v1 — do not build without owner approval)

- Migration apply command; live-DB drift check (`hejbro check`)
- Hybrid authoring (real TS arrows for single-expression SQL functions)
- Client type generation; Neon / Nile presets
- ESLint plugin against real-JS control flow (v1.x, after Phase 3 knows the
  exact failure modes)
