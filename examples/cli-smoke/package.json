{
	"name": "cli-smoke",
	"version": "0.0.0",
	"private": true,
	"description": "Task 18 end-to-end acceptance: drives the built hejbro CLI (init/generate/verify) against a real schema, in a tmp copy.",
	"type": "module",
	"scripts": {
		"test": "vitest run"
	},
	"dependencies": {
		"hejbro": "workspace:*"
	},
	"devDependencies": {
		"vitest": "catalog:"
	}
}
