{
	"name": "dd-land",
	"version": "0.0.0",
	"private": true,
	"description": "Task 18 acceptance: a reduced dd.land schema exercising every Supabase preset feature once (D44) — authUsers FK, authUid() RLS policy, storage bucket, role-preset grant, the exposed-table warning, and the view security-invoker warning (#66).",
	"type": "module",
	"scripts": {
		"check-types": "tsc --noEmit",
		"test": "vitest run"
	},
	"dependencies": {
		"@hejbro/core": "workspace:*",
		"@hejbro/supabase": "workspace:*"
	},
	"devDependencies": {
		"typescript": "catalog:",
		"vitest": "catalog:"
	}
}
