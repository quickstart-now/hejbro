{
	"name": "example-supabase",
	"version": "0.0.0",
	"private": true,
	"description": "Showcase: the Supabase preset on a generic schema \u2014 authUsers FK, authUid() RLS, a storage bucket, role-preset grants, the exposed-table warning, the view security-invoker warning \u2014 with a five-step migration history (the fifth declares the platform-owned auth.users with existingTable()), seed data, and a local Docker round-trip.",
	"type": "module",
	"scripts": {
		"check-types": "tsc --noEmit",
		"test": "vitest run",
		"roundtrip": "bash ../../scripts/roundtrip.sh . seed/supabase.sql"
	},
	"dependencies": {
		"@hejbro/core": "workspace:*",
		"@hejbro/supabase": "workspace:*",
		"hejbro": "workspace:*"
	},
	"devDependencies": {
		"typescript": "catalog:",
		"vitest": "catalog:"
	}
}
